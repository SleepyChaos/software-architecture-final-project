# 图书管理系统 — 项目技术栈应用说明文档

## 系统概述

本项目是一个前后端分离的图书管理系统，采用微服务化架构设计，涵盖用户登录认证、图书搜索与推荐、数据统计分析、系统监控等核心功能。系统通过 Docker 容器化打包，支持 Docker Compose 本地编排和 Kubernetes 生产级部署，并集成了完整的 CI/CD 流水线和自动化测试体系。

---

## 1. Vue 3 — 前端框架

**系统位置：** 前端 SPA 应用核心框架，位于 `src/` 目录下的所有 `.vue` 组件文件。

**使用方式与原理：**

Vue 3 作为前端核心框架，负责构建整个单页面应用（SPA）。项目使用 Vue 3 的 Composition API（`<script setup>`）编写组件，通过响应式数据绑定（`ref`、`computed`）实现数据驱动视图更新。

- **组件化开发：** 系统将页面拆分为多个独立组件，如 `App.vue`（根组件）、`Home.vue`（首页）、`Search.vue`（搜索页）、`Login.vue`（登录页）等，每个组件封装自己的模板、逻辑和样式。
- **路由管理：** 通过 Vue Router 实现客户端路由，在 `src/router/index.ts` 中定义了所有页面路由规则，并使用 `createWebHistory()` 实现 HTML5 History 模式的无刷新导航。路由守卫 `router.beforeEach` 实现了页面标题设置和登录权限验证。
- **状态管理：** 使用 Pinia（Vue 3 官方推荐）管理全局状态，如 `src/stores/auth.ts` 管理用户认证状态，`src/stores/books.ts` 管理图书数据和搜索逻辑。
- **前后端通信：** 组件中通过 `fetch` API 调用后端接口（如 `/api/login`、`/api/books/search`），获取数据后更新到响应式状态中，自动触发视图重新渲染。

---

## 2. FastAPI — 后端 Web 框架

**系统位置：** 后端 API 服务入口，位于 `backbone/main.py`。

**使用方式与原理：**

FastAPI 是 Python 的高性能异步 Web 框架，负责构建后端所有 RESTful API 接口。项目通过 Uvicorn 作为 ASGI 服务器运行 FastAPI 应用。

- **路由定义：** 在 `main.py` 中通过装饰器定义所有 API 端点：
  - `POST /login` — 用户登录，返回 JWT 令牌
  - `GET /books/recommended` — 推荐图书
  - `GET /books/search` — 图书搜索
  - `GET /search/suggestions` — 搜索建议
  - `GET /analytics/search-trends` — 搜索趋势统计
  - `GET /healthz` — 健康检查
  - `GET /metrics` — Prometheus 监控指标暴露
- **中间件机制：** 注册了两个 HTTP 中间件：
  - `prometheus_metrics_middleware`：拦截每个请求，记录请求计数（Counter）和延迟（Histogram）到 Prometheus 指标。
  - `rate_limit_middleware`：基于 Redis 的 IP 限流，防止 API 滥用（100次/60秒）。
- **依赖注入：** 使用 FastAPI 的 `Depends()` 机制实现数据库会话注入（`get_db`）和用户认证（`get_current_user`），实现了请求级别资源的自动管理和清理。
- **CORS 配置：** 通过 `CORSMiddleware` 允许前端跨域访问后端 API，解决前后端分离架构下的跨域问题。
- **自动文档：** FastAPI 自动生成 Swagger UI（`/docs`）和 ReDoc（`/redoc`）接口文档，便于调试。

---

## 3. SQLAlchemy — 数据库 ORM

**系统位置：** 后端数据持久层，位于 `backbone/database.py` 和 `backbone/models.py`。

**使用方式与原理：**

SQLAlchemy 是 Python 的 ORM（对象关系映射）框架，负责将 Python 对象映射到 PostgreSQL 数据库表，屏蔽底层 SQL 操作。

- **引擎创建（`database.py`）：** 通过 `create_engine(DATABASE_URL)` 创建数据库连接引擎，`DATABASE_URL` 指向 PostgreSQL 数据库（`postgresql://library:library@db:5432/library`）。
- **会话管理（`database.py`）：** 使用 `sessionmaker` 创建会话工厂 `SessionLocal`，配置 `autocommit=False`（手动提交事务）和 `autoflush=False`（手动控制刷新时机）。`get_db()` 是一个生成器函数，通过 `yield` 实现会话的自动创建和关闭，配合 FastAPI 的依赖注入使用。
- **数据模型（`models.py`）：** 定义 `User` 模型类，继承自 `Base`（`declarative_base()`），映射到 `users` 表：
  - `reader_id`：读者号码，主键 + 索引
  - `hashed_password`：加密后的密码
- **ORM 查询：** 在 `auth.py` 的 `authenticate_user` 函数中，通过 `db.query(models.User).filter(...)` 执行 ORM 查询，自动将 SQL 转换为 Python 对象操作。
- **测试替换：** 在测试环境中（`conftest.py`），将 PostgreSQL 替换为 SQLite 内存数据库（`sqlite:///:memory:`），使用 `StaticPool` 共享连接，实现测试隔离。

---

## 4. Redis — 内存缓存数据库

**系统位置：** 后端缓存层 + 限流层 + 分析数据层，主要位于 `backbone/main.py` 的 `read_cache`/`write_cache`/`is_rate_limited` 函数，以及 `backbone/rabbitmq_worker.py` 中的热词统计。

**使用方式与原理：**

Redis 作为高速内存数据库，在系统中承担三重角色：

**（1）API 响应缓存（`main.py`）：**
- **推荐图书缓存：** `GET /books/recommended` 接口首先查询 Redis 键 `books:recommended`，命中则直接返回缓存数据，未命中则计算结果并通过 `setex` 写入缓存（TTL=300秒），减少重复计算。
- **搜索建议缓存：** `GET /search/suggestions` 接口按查询词缓存结果到 `search:suggestions:{keyword}`，TTL=180秒。
- **搜索结果缓存：** `GET /books/search` 接口按查询参数组合生成缓存键（如 `books:search:红楼梦:科技:available:12`），TTL=300秒。
- 缓存数据以 JSON 序列化存储，通过 `json.dumps`/`json.loads` 进行编解码。

**（2）IP 限流（`main.py` 的 `is_rate_limited`）：**
- 使用 Redis 的 `INCR` + `EXPIRE` 实现滑动窗口限流，每个 IP 在 60 秒内最多 100 次请求。Redis 的原子操作保证了并发安全。

**（3）分析数据存储（`rabbitmq_worker.py`）：**
- 搜索热词统计：使用 Redis 的有序集合（Sorted Set）`analytics:search_terms`，通过 `ZINCRBY` 命令累加搜索词频次，`ZREVRANGE` 获取 Top N 热词。
- 登录计数：通过 `INCR` 命令分别统计登录成功（`analytics:login_success_total`）和失败（`analytics:login_failed_total`）次数。

**降级策略：** 所有 Redis 操作均包裹在 try/except 中，Redis 不可用时自动降级到本地计算逻辑，保证系统可用性。

---

## 5. Elasticsearch — 搜索引擎

**系统位置：** 后端搜索服务层，位于 `backbone/search_index.py`。

**使用方式与原理：**

Elasticsearch 是分布式全文搜索引擎，在系统中负责图书的全文检索和搜索建议功能。

- **索引管理（`ensure_index`）：** 应用启动时自动创建 `library-books` 索引，定义字段映射（mapping）：
  - `title`、`author`、`description` 设为 `text` 类型，启用全文分词分析
  - `category`、`isbn` 设为 `keyword` 类型，用于精确匹配
  - `availableCopies`、`publishYear` 设为 `integer` 类型，用于范围过滤
  - 通过 `helpers.bulk` 批量将图书数据写入索引
- **图书搜索（`search_books`）：** 使用 Elasticsearch 的 `bool` 复合查询：
  - `must` 子句：`multi_match` 在 `title`（权重 ×4）、`author`（×2）、`isbn`（×3）、`description` 四个字段上进行最佳匹配搜索，支持模糊匹配（`fuzziness: AUTO`）
  - `filter` 子句：按分类（`term`）和可借数量（`range`）进行过滤
  - 结果按相关性评分 `_score` + 可借数量 + 出版年份排序
- **搜索建议（`suggest_books`）：** 使用 `bool_prefix` 类型的 `multi_match` 查询，实现输入前缀匹配书名，提供实时搜索建议。
- **回退机制：** Elasticsearch 不可用时（连接超时、查询异常），自动回退到 `_fallback_search` 本地搜索函数，通过字符串包含匹配在内存数据中搜索。

---

## 6. RabbitMQ — 消息队列

**系统位置：** 后端事件发布层 `backbone/message_queue.py` + 异步消费层 `backbone/rabbitmq_worker.py`。

**使用方式与原理：**

RabbitMQ 是消息中间件，在系统中实现事件驱动的异步处理架构，解耦主服务与统计分析服务。

- **事件发布（`message_queue.py` 的 `publish_event`）：**
  - 使用 `pika` 库（RabbitMQ 的 Python 客户端）建立连接
  - 声明持久化队列 `library.events`（`durable=True`）
  - 发布消息时设置 `delivery_mode=2`（消息持久化），确保 RabbitMQ 重启后消息不丢失
  - 消息体为 JSON 格式，包含 `event_type`（事件类型）、`payload`（数据载荷）、`created_at`（时间戳）
- **发布时机（`main.py`）：**
  - `auth.login_success` / `auth.login_failed`：用户登录成功/失败时发布
  - `search.books`：每次图书搜索时发布（包含搜索词、分类、结果数量）
- **事件消费（`rabbitmq_worker.py`）：**
  - 独立的 Worker 进程持续监听队列（`start_consuming`）
  - `handle_message` 回调函数根据事件类型将数据写入 Redis：搜索词通过 `ZINCRBY` 累加热词，登录事件通过 `INCR` 累加计数
  - 使用 `basic_qos(prefetch_count=1)` 实现公平分发，每次只处理一条消息
  - 消费完成后通过 `basic_ack` 手动确认，确保消息处理的可靠性（at-least-once 语义）
  - Worker 进程包含自动重连机制，连接断开后等待 3 秒后重试
- **熔断保护：** 所有 RabbitMQ 操作通过 `rabbitmq_breaker` 熔断器保护，队列不可用时不影响主业务流程。

---

## 7. ECharts — 数据可视化库

**系统位置：** 前端数据统计页面，位于 `src/views/Statistics.vue`。

**使用方式与原理：**

ECharts 是百度开源的数据可视化图表库，通过 `vue-echarts` 封装组件集成到 Vue 3 中，用于数据统计页面的图表渲染。

- **按需引入：** 在 `Statistics.vue` 中通过 `echarts/core` 按需引入所需模块：
  - `PieChart`（饼图）、`BarChart`（柱状图）— 图表类型
  - `TitleComponent`、`TooltipComponent`、`LegendComponent`、`GridComponent` — 交互组件
  - `CanvasRenderer` — Canvas 渲染器
- **三大图表：**
  - **图书分类占比（饼图）：** 基于前端 Pinia Store 中的图书数据，通过 `computed` 计算各分类数量，渲染环形饼图，支持 hover 高亮和 tooltip 展示。
  - **月度借阅量（柱状图）：** 展示近 6 个月的借阅量趋势，使用 X 轴类目轴 + Y 轴数值轴。
  - **热门搜索词 Top 10（横向柱状图）：** 从后端 API `/api/analytics/search-trends` 获取真实搜索热词数据（来源于 Redis Sorted Set + RabbitMQ Worker 统计），数据不可用时使用本地模拟数据降级显示。
- **响应式设计：** 通过 `vue-echarts` 的 `autoresize` 属性，图表自动适配容器大小变化。

---

## 8. Prometheus — 监控系统

**系统位置：** 后端指标采集（`backbone/main.py`）+ 指标暴露端点（`GET /metrics`）+ Prometheus 配置（`prometheus.yml`）。

**使用方式与原理：**

Prometheus 是开源的系统监控和告警工具，在本项目中用于采集和存储后端服务的运行时指标。

- **指标定义（`main.py`）：** 使用 `prometheus_client` 库定义了三个核心指标：
  - `library_http_requests_total`（Counter）：HTTP 请求总数，按 `method`、`endpoint`、`status` 三个维度（label）分类统计。
  - `library_http_request_duration_seconds`（Histogram）：HTTP 请求延迟分布，按 `method`、`endpoint` 分类统计。
  - `library_cache_operations_total`（Counter）：Redis 缓存操作计数，按 `cache_name`、`operation`（hit/miss/store/error）分类统计。
- **中间件自动采集：** `prometheus_metrics_middleware` 中间件拦截每个 HTTP 请求，使用 `time.perf_counter()` 精确计算请求耗时，在请求完成后自动累加 Counter 和观测 Histogram。
- **指标暴露：** `GET /metrics` 端点调用 `generate_latest()` 生成 Prometheus 文本格式的指标数据，供 Prometheus 服务端定期拉取（scrape）。
- **Prometheus 配置（`prometheus.yml`）：** 配置全局抓取间隔为 15 秒，抓取目标为 `backend:8000` 的 `/metrics` 路径。
- **监控闭环：** Prometheus 采集到的指标可以在 Grafana 等可视化工具中展示，实现 QPS、延迟、缓存命中率等关键性能指标的实时监控。

---

## 9. Nginx — Web 服务器与反向代理

**系统位置：** 前端容器内的 Web 服务器，配置位于 `nginx.conf`，Docker 镜像定义在根目录 `Dockerfile`。

**使用方式与原理：**

Nginx 在系统中承担双重角色：静态资源服务器和 API 反向代理。

- **静态资源服务：** 前端 Vue 应用通过 Vite 构建后生成 `dist/` 目录的静态文件（HTML/CSS/JS），由 Nginx 直接提供服务：
  - `root /usr/share/nginx/html` 指定静态文件根目录
  - `location /assets/` 匹配静态资源请求，直接返回文件
  - `location /` 使用 `try_files $uri $uri/ /index.html` 实现 SPA 的 History 模式路由回退——当请求的路径不对应任何静态文件时，统一返回 `index.html`，由前端 Vue Router 接管路由。
- **API 反向代理：**
  - `location /api/` 匹配所有 `/api/` 前缀的请求，通过 `proxy_pass http://backend_pool/` 转发到后端服务。
  - 配置了请求头透传：`X-Real-IP`（真实客户端 IP）、`X-Forwarded-For`（代理链 IP）、`X-Forwarded-Proto`（原始协议），确保后端能获取到真实的客户端信息。
  - `proxy_http_version 1.1` 启用 HTTP/1.1 协议以支持长连接。
- **Docker DNS 解析器：** `resolver 127.0.0.11 valid=30s` 使用 Docker 内置 DNS 解析器，每 30 秒刷新一次后端容器的 IP 地址，适应容器动态调度。
- **容器化部署：** 前端 Dockerfile 基于 `nginx:1.27-alpine` 镜像，将 `nginx.conf` 和 `dist/` 复制进容器，暴露 80 端口。

---

## 10. 负载均衡 — 流量分发机制

**系统位置：** Nginx 上游配置（`nginx.conf` 的 `upstream` 块）+ Kubernetes 服务配置（`k8s/backend-service.yaml`、`k8s/backend-deployment.yaml`）。

**使用方式与原理：**

系统通过两层负载均衡实现高可用和流量分发：

**（1）Nginx 层负载均衡：**
- `upstream backend_pool` 块定义了后端服务池，将请求分发到后端实例。
- 在 Docker Compose 环境中，Nginx 通过 Docker DNS 解析 `backend` 主机名，自动发现后端容器。

**（2）Kubernetes 层负载均衡：**
- `backend-deployment.yaml` 配置 `replicas: 3`，Kubernetes 会运行 3 个后端 Pod 副本。
- Kubernetes Service（`backend-service.yaml`）自动在多个 Pod 之间进行负载均衡，使用标签选择器 `app: library-backend` 发现后端 Pod。
- Kubernetes 的 Service 机制提供了内置的 L4 负载均衡能力，请求会被自动分发到健康的 Pod 上。

**（3）健康检查保障：**
- K8s Deployment 配置了 `livenessProbe`（存活探针）和 `readinessProbe`（就绪探针），均请求 `/healthz` 端点。
- 存活探针：每 15 秒检测一次，失败则自动重启 Pod。
- 就绪探针：每 10 秒检测一次，失败则从 Service 的负载均衡池中摘除该 Pod，确保流量只路由到健康实例。

---

## 11. Docker — 容器化平台

**系统位置：** 前端 Dockerfile（根目录 `Dockerfile`）、后端 Dockerfile（`backbone/Dockerfile`）、编排文件（`docker-compose.yml`）。

**使用方式与原理：**

Docker 将应用及其运行环境打包为标准化容器镜像，实现"构建一次，到处运行"。

- **前端镜像（`Dockerfile`）：**
  - 基础镜像：`nginx:1.27-alpine`（轻量级 Alpine Linux）
  - 构建过程：将 Nginx 配置和 Vite 构建产物复制到镜像中
  - 暴露端口：80
- **后端镜像（`backbone/Dockerfile`）：**
  - 基础镜像：`python:3.11-slim`
  - 构建过程：先安装 Python 依赖（`pip install -r requirements.txt`），再复制后端代码
  - 暴露端口：8000
  - 运行命令：`uvicorn main:app --host 0.0.0.0 --port 8000`
- **Docker Compose 编排（`docker-compose.yml`）：**
  - 定义了 7 个服务容器：`frontend`（前端）、`backend`（后端 API）、`rabbitmq-worker`（消息消费者）、`db`（PostgreSQL）、`redis`、`elasticsearch`、`rabbitmq`、`prometheus`
  - 服务依赖管理：`backend` 依赖 `db`（健康检查通过后启动）、`redis`、`elasticsearch`、`rabbitmq`
  - 环境变量注入：通过 `environment` 字段为每个服务注入连接地址、缓存 TTL 等配置
  - 数据持久化：使用命名卷 `pgdata` 持久化 PostgreSQL 数据，`prometheus-data` 持久化 Prometheus 时序数据
  - 网络隔离：所有服务在同一 Docker 网络中，通过服务名（如 `db`、`redis`）互相访问

---

## 12. Kubernetes — 容器编排系统

**系统位置：** `k8s/` 目录下的所有 YAML 清单文件，涵盖 Deployment、Service、ConfigMap、PVC 等资源。

**使用方式与原理：**

Kubernetes（K8s）是生产级容器编排平台，负责自动化部署、扩缩容和服务发现。

- **Deployment（工作负载）：**
  - `backend-deployment.yaml`：定义后端 Deployment，`replicas: 3` 运行 3 个 Pod 副本，配置存活探针和就绪探针，通过 `envFrom` 引用 ConfigMap 注入环境变量。
  - `frontend-deployment.yaml`、`elasticsearch-deployment.yaml`、`postgres-deployment.yaml`、`redis-deployment.yaml`、`rabbitmq-deployment.yaml`、`prometheus-deployment.yaml`：分别定义各服务的 Deployment。
- **Service（服务发现与负载均衡）：**
  - 每个 Deployment 配套一个 Service（如 `backend-service.yaml`），通过标签选择器关联 Pod，提供稳定的 ClusterIP 访问入口。
- **ConfigMap（配置管理）：**
  - `backend-configmap.yaml`：存储后端环境变量（数据库连接、Redis 地址等），与代码解耦。
  - `elasticsearch-configmap.yaml`、`prometheus-configmap.yaml`：存储各中间件配置。
- **PVC（持久化存储）：**
  - `postgres-pvc.yaml`：为 PostgreSQL 申请持久化存储卷，确保数据库数据不因 Pod 重启而丢失。
- **CI 校验：** GitHub Actions 的 `k8s-validate` Job 自动校验所有 K8s YAML 文件的语法正确性（检查 `kind`、`apiVersion` 字段）和资源完整性。

---

## 13. PyJWT — JSON Web Token 认证库

**系统位置：** 后端认证模块，位于 `backbone/auth.py`。

**使用方式与原理：**

PyJWT 用于生成和验证 JWT（JSON Web Token），实现无状态的用户身份认证。

- **令牌生成（`create_access_token`）：**
  - 接收用户数据字典（如 `{"sub": "001"}`）和过期时间（默认 30 分钟）
  - 将 `exp`（过期时间戳）写入 payload
  - 使用 `jwt.encode()` 以 HS256 算法和预定义的 `SECRET_KEY` 对 payload 进行签名，生成 JWT 字符串
  - 登录成功后返回给前端：`{"access_token": "...", "token_type": "bearer"}`
- **令牌验证（`get_current_user`）：**
  - 作为 FastAPI 的依赖注入函数，通过 `OAuth2PasswordBearer` 从请求头 `Authorization: Bearer <token>` 中提取令牌
  - 使用 `jwt.decode()` 以相同的密钥和算法验证令牌签名，提取 `sub` 字段（reader_id）
  - 令牌过期或签名无效时抛出 401 未授权异常
  - 验证通过后从数据库查询用户信息，确保用户存在
- **认证流程：** 前端登录 → 获取 JWT → 存储在 localStorage → 后续请求携带 JWT → 后端验证 → 允许访问受保护资源。

---

## 14. Passlib — 密码哈希库

**系统位置：** 后端认证模块，位于 `backbone/auth.py`。

**使用方式与原理：**

Passlib 是 Python 的密码哈希框架，配合 bcrypt 算法实现安全的密码存储，防止密码明文泄露。

- **哈希上下文初始化：** `pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")`
  - 使用 bcrypt 作为唯一哈希方案
  - `deprecated="auto"` 表示自动处理旧版哈希算法的迁移
- **密码加密（`get_password_hash`）：**
  - 调用 `pwd_context.hash(password)` 对明文密码进行 bcrypt 哈希
  - bcrypt 自动生成随机盐值（salt），每次加密同一密码都会产生不同的哈希值，防止彩虹表攻击
  - 在应用启动时（`startup_event`），使用此函数为演示用户创建哈希密码并存入数据库
- **密码验证（`verify_password`）：**
  - 调用 `pwd_context.verify(plain_password, hashed_password)` 验证明文密码是否与存储的哈希匹配
  - 在 `authenticate_user` 中被调用：先从数据库查询用户，再验证密码
- **安全设计：** 数据库中只存储哈希值（`hashed_password` 字段），即使数据库被攻破，攻击者也无法直接获取用户密码。

---

## 15. pybreaker — 断路器模式实现

**系统位置：** 后端容错层，位于 `backbone/circuit_breaker.py`，被 `main.py` 中的所有外部服务调用使用。

**使用方式与原理：**

pybreaker 实现了断路器（Circuit Breaker）设计模式，防止外部服务故障引发级联失败，提高系统容错性。

- **三个独立断路器：**
  - `redis_breaker`：保护 Redis 操作，最大失败次数 5 次，恢复超时 30 秒
  - `elasticsearch_breaker`：保护 Elasticsearch 操作，最大失败次数 3 次，恢复超时 60 秒
  - `rabbitmq_breaker`：保护 RabbitMQ 操作，最大失败次数 3 次，恢复超时 45 秒
- **三态工作原理：**
  - **Closed（闭合）：** 正常状态，请求正常通过。失败计数累加，达到阈值后切换到 Open 状态。
  - **Open（断开）：** 熔断状态，所有请求直接抛出异常，不实际调用外部服务，避免资源浪费和级联故障。经过 `reset_timeout` 秒后自动切换到 Half-Open。
  - **Half-Open（半开）：** 试探状态，允许一个请求通过。成功则恢复为 Closed，失败则回到 Open。
- **状态监听：** 每个断路器注册了 `state_change_listener`，状态切换时通过 `logging` 模块记录日志，便于排查问题。
- **使用方式：** 在 `main.py` 中以装饰器形式使用，如 `@redis_breaker` 包裹 Redis 缓存读写操作，`@elasticsearch_breaker` 包裹 ES 搜索操作，`@rabbitmq_breaker` 包裹消息发布操作。
- **健康检查集成：** `GET /healthz` 端点通过 `get_all_circuit_breaker_states()` 返回所有断路器的当前状态，运维人员可以直接查看各外部服务的健康状况。

---

## 16. pytest — 测试框架

**系统位置：** 后端测试套件，位于 `backbone/tests/` 目录，配置文件 `backbone/pytest.ini`。

**使用方式与原理：**

pytest 是 Python 最流行的测试框架，负责后端所有层级的自动化测试。配合 pytest-asyncio 支持异步测试。

- **测试分层（通过 pytest mark 标记）：**
  - **单元测试（`@pytest.mark.unit`）：** 测试纯函数逻辑，如 `test_cache_logic.py`（缓存读写逻辑）、`test_schemas.py`（Pydantic 模型验证）、`test_mock_data.py`（模拟数据完整性）、`test_worker_handler.py`（RabbitMQ Worker 消息处理）、`test_search_fallback.py`（搜索回退逻辑）。
  - **API 功能测试（`@pytest.mark.api`）：** 测试 HTTP 接口行为，如 `test_api_login.py`（登录接口）、`test_api_books.py`（图书接口）、`test_api_search.py`（搜索接口）、`test_api_healthz.py`（健康检查）、`test_api_root.py`（根路由）、`test_api_metrics.py`（指标端点）、`test_api_analytics.py`（分析接口）、`test_api_suggestions.py`（搜索建议）。
  - **集成测试（`@pytest.mark.integration`）：** 需要真实外部服务，如 `test_integration_db.py`（PostgreSQL）、`test_integration_redis.py`（Redis）、`test_integration_es.py`（Elasticsearch）、`test_integration_rabbitmq.py`（RabbitMQ）。
  - **负载测试（`@pytest.mark.load`）：** `test_load_api.py` 验证系统在并发压力下的响应表现。
- **Fixture 体系（`conftest.py`）：**
  - `sqlite_engine` / `sqlite_session_factory`：创建 SQLite 内存数据库，session 级别复用
  - `db_session`：每个测试独立的数据库会话，自动回滚隔离
  - `test_app` / `client`：创建测试用 FastAPI 应用，替换数据库为 SQLite，Mock 所有外部服务（Redis 返回 None、RabbitMQ 不可用、ES 不可用）
  - `fake_redis`：提供 fakeredis 实例用于缓存逻辑测试
  - `seed_user`：在测试数据库中插入测试用户
- **运行命令：** CI 中通过 `python -m pytest tests/ -m unit -v` 分别运行不同标记的测试。

---

## 17. logging — 日志记录模块

**系统位置：** 贯穿整个后端代码，核心配置位于 `backbone/circuit_breaker.py`，使用于各业务模块。

**使用方式与原理：**

Python 标准库的 `logging` 模块是项目中统一的日志输出方案，用于记录系统运行状态、错误信息和关键事件。

- **日志配置：** 在 `circuit_breaker.py` 中通过 `logging.basicConfig(level=logging.INFO)` 设置全局日志级别为 INFO，使用 `logging.getLogger(__name__)` 创建命名 logger。
- **断路器状态日志：** 断路器状态切换时记录日志，如 `"Redis circuit breaker changed from closed to open"`，帮助运维人员追踪服务降级时间线。
- **异常日志：** 断路器捕获异常时记录 `logger.error(f"Circuit breaker {breaker_name} caught exception: {e}")`，包含异常详情便于问题定位。
- **测试日志：** 测试的 `conftest.py` 中同样使用 logging 记录 fixture 生命周期事件（如 "SQLite 内存数据库引擎已创建"、"TestClient 已就绪"），便于调试测试问题。
- **压测日志：** Locust 压测脚本中通过 `logger.info` 记录用户登录成功/失败事件。
- **项目规范：** 项目统一使用 `logging` 模块输出日志，禁止使用 `print` 语句（仅少量遗留的 print 用于启动信息输出）。

---

## 18. GitHub Actions — CI/CD 持续集成

**系统位置：** CI/CD 配置文件，位于 `.github/workflows/ci.yml`。

**使用方式与原理：**

GitHub Actions 是 GitHub 内置的 CI/CD 平台，本项目配置了完整的自动化流水线，在每次代码推送（push）和 Pull Request 时自动执行。

- **触发条件：** 推送至 `main`/`master`/`develop` 分支，或对 `main`/`master` 的 PR。
- **10 个并行/串行 Job：**
  1. **frontend** — 前端检查与构建：ESLint 代码风格检查 → TypeScript 类型检查（`vue-tsc`）→ Vite 构建 → 验证 dist 产物 → 上传构建制品。
  2. **backend** — 后端检查：Python 语法编译检查（`compileall`）→ Flake8 代码风格检查 → 验证所有核心依赖包可正常导入。
  3. **backend-tests** — 后端单元测试 + API 功能测试：安装依赖 → 运行 `pytest -m unit` → 运行 `pytest -m api`。
  4. **integration-tests** — 集成测试：构建前端 → 启动 Docker Compose 全套服务 → 等待后端就绪 → 运行 `pytest -m integration` → 运行 `pytest -m load`（负载测试）→ 清理容器。
  5. **docker-frontend** — 前端 Docker 镜像构建验证：下载前端构建制品 → 使用 Buildx 构建 Docker 镜像 → 验证镜像创建成功。
  6. **docker-backend** — 后端 Docker 镜像构建验证：同上。
  7. **docker-compose** — Docker Compose 全栈编排验证：校验 `docker-compose.yml` 语法 → 构建所有服务镜像。
  8. **k8s-validate** — Kubernetes 清单校验：使用 Python yaml 模块校验所有 K8s YAML 文件的语法和资源定义完整性。
  9. **prometheus-validate** — Prometheus 配置校验：验证 `prometheus.yml` 包含 `global` 和 `scrape_configs` 配置。
  10. **nginx-validate** — Nginx 配置校验：验证 `nginx.conf` 包含关键指令（upstream、server、proxy_pass 等）和大括号匹配。
  11. **summary** — 流水线汇总：聚合所有 Job 状态，任一失败则整体标记为失败。
- **依赖关系：** 通过 `needs` 关键字定义 Job 之间的依赖，如 `backend-tests` 依赖 `backend` 通过后执行，`docker-frontend` 依赖 `frontend` 构建成功后执行。
- **缓存优化：** 使用 `actions/cache` 缓存 npm 和 pip 依赖，使用 Docker Buildx 的 GHA 缓存加速镜像构建。

---

## 19. Locust — 性能测试工具

**系统位置：** 后端压测脚本，位于 `backbone/tests/locustfile.py`。

**使用方式与原理：**

Locust 是基于 Python 的分布式负载测试工具，用于模拟真实用户并发访问，评估后端 API 的性能表现。

- **用户行为定义（`LibraryUser` 类）：**
  - 继承 `HttpUser`，定义模拟用户的基本行为
  - `wait_time = between(0.5, 2)`：每次请求之间随机等待 0.5~2 秒，模拟真实用户操作间隔
- **登录初始化（`on_start`）：**
  - 每个虚拟用户启动时自动执行登录，随机选择测试账号（`001/123456` 或 `face-user/face`）
  - 登录成功记录成功日志，失败记录失败信息
- **7 个任务及权重：**
  - `search_books`（权重 3）：随机关键词搜索图书
  - `search_with_category`（权重 2）：带分类过滤的搜索
  - `search_available_only`（权重 2）：仅搜索可借图书
  - `get_recommended`（权重 4）：获取推荐图书（最高频操作）
  - `get_suggestions`（权重 3）：获取搜索建议
  - `get_search_trends`（权重 1）：获取搜索趋势
  - `health_check`（权重 1）：健康检查
  - `login_retry`（权重 1）：模拟用户重新登录
  - `root_page`（权重 1）：访问根路由
- **运行方式：**
  - Web UI 模式：`locust -f locustfile.py --host http://127.0.0.1:8000`，浏览器打开 `http://127.0.0.1:8089` 可视化配置用户数和观察结果
  - 无头模式：`locust --headless -u 50 -r 5 -t 30s` 模拟 50 个并发用户，每秒新增 5 个，持续 30 秒
- **CI 集成：** 在 GitHub Actions 的 `integration-tests` Job 中，通过 `pytest -m load` 运行 `test_load_api.py`，调用 Locust 进行自动化负载测试，验证系统在压力下的响应时间和吞吐量是否满足阈值要求。
- **关键指标：** Locust 输出 RPS（每秒请求数）、响应时间百分位（P50/P95/P99）、失败率等性能指标，用于评估系统容量和优化瓶颈。
