# K8s + Docker + Redis 技术栈增加计划

## 一、总体目标

在不推翻现有项目结构的前提下，为图书管理系统增加以下工程化能力：

- 使用 Docker 完成前端、后端、Redis 的容器化部署
- 使用 Kubernetes 对容器进行轻量编排
- 使用 Redis 为首页推荐和搜索建议提供缓存支持

本次改造强调两点：

- 能跑起来：本地具备可验证、可演示的部署方案
- 能讲清楚：实验报告中能够说明技术选择原因、实际作用与后续扩展价值

## 二、最终选型

- 主技术栈：Docker + Kubernetes
- 附加技术栈：Redis

## 三、项目中的技术定位

### 1. Docker 的定位

Docker 作为底层运行载体，负责将当前系统拆分为多个标准化运行单元：

- 前端静态站点容器
- FastAPI 后端容器
- Redis 缓存容器

这样做的价值在于：

- 统一环境
- 减少本地运行差异
- 为 Kubernetes 编排提供标准镜像

### 2. Kubernetes 的定位

Kubernetes 在本项目中不作为“高并发生产级集群平台”使用，而是作为轻量化编排工具使用。

本次使用方式强调：

- 使用 Deployment 管理前端、后端、Redis 三类服务
- 使用 Service 进行服务暴露与内部通信
- 使用 ConfigMap 管理后端配置
- 使用 PVC 保存 SQLite 数据
- 使用健康检查体现平台级管理能力

报告中需要明确说明：

当前项目规模较小，确实不需要重型容器编排平台，但先引入 Kubernetes 可以让项目具备规范的服务管理骨架，为未来数据库升级、多副本部署和服务扩容打下基础。

### 3. Redis 的定位

Redis 在本项目中的首版职责仅限于缓存，不扩展到会话共享、消息队列或分布式锁。

优先落地场景：

- 首页推荐图书缓存
- 搜索建议缓存

这样做的优点是：

- 改动点少
- 对现有前端结构影响小
- 能在实验展示中直观体现“引入中间件后接口访问效率和工程层次的提升”

## 四、实施路线

### 阶段一：文档归档

目标：

- 固化技术选型理由
- 为后续实现提供统一说明口径

产出：

- `技术栈适配性分析.md`
- 本计划文档

### 阶段二：前后端本地联通

目标：

- 去掉前端登录对公网地址的硬编码依赖
- 改为连接本地 FastAPI 后端

实施要点：

- 增加前端环境变量 `VITE_API_BASE_URL`
- 开发环境默认指向本地后端
- 容器环境走同源 `/api`
- 保留原有登录响应结构
- 补充演示账号，兼容读者证登录与人脸模拟登录

### 阶段三：Docker 容器化

目标：

- 将前端、后端、Redis 打包为可统一启动的容器系统

实施要点：

- 前端使用 Vite 构建后交给 Nginx 托管
- 后端使用 Python 轻量镜像运行 FastAPI
- Redis 使用官方镜像
- 使用 `docker-compose` 管理多容器运行
- 将 SQLite 数据挂载持久化卷，避免重建容器后数据丢失

### 阶段四：Redis 缓存接入

目标：

- 为推荐与搜索建议接口增加缓存能力

实施要点：

- 后端新增推荐接口与搜索建议接口
- Redis 命中时直接返回缓存结果
- Redis 未命中时从后端 mock 数据生成结果并回填缓存
- Redis 不可用时自动降级为直接计算结果，保证系统可用性

### 阶段五：Kubernetes 编排

目标：

- 将 Docker 化后的前端、后端、Redis 进一步编排成标准化服务结构

实施要点：

- `frontend`、`backend`、`redis` 各自使用 Deployment
- 全部先使用单副本
- 通过 ClusterIP 进行集群内部访问
- 本地演示使用 `kubectl port-forward`
- `backend` 配置健康检查
- `backend` 使用 PVC 保存 SQLite 数据

### 阶段六：README 与部署说明收口

目标：

- 形成完整可交付的项目说明

实施要点：

- 增加原生运行方式说明
- 增加 Docker Compose 运行说明
- 增加 K8s 本地部署说明
- 增加 Redis 能力说明
- 增加当前方案限制和后续扩展方向

## 五、实现边界

本次明确不做以下内容：

- 不切换数据库为 MySQL 或 PostgreSQL
- 不实现真正的读写分离
- 不拆分微服务
- 不引入 Sentinel、Nacos、ZooKeeper 等重型配套组件
- 不将所有业务一次性迁移到完整后端

这样控制范围的原因是：

- 当前项目仍以课程作业和演示为主
- 需要在有限时间内完成“可实现、可运行、可说明”的技术增强

## 六、实验报告可用表述

### 1. 为什么选择 K8s + Docker

可以在报告中表述为：

虽然当前系统规模较轻，不需要复杂的高并发调度平台，但通过 Docker 对系统进行容器化，再使用 Kubernetes 进行基础编排，可以提前构建统一的服务管理结构，使项目具备标准化部署、服务治理和后续扩容的技术基础。

### 2. 为什么 Redis 选择缓存而不是更复杂能力

可以在报告中表述为：

当前系统中首页推荐和搜索建议属于典型的高频读取场景，采用 Redis 进行缓存能够在较小改动范围内提升系统响应效率，并体现中间件在性能优化中的实际作用，因此首版将 Redis 定位为缓存组件，而非登录共享或分布式协调组件。

### 3. 为什么 K8s 仍然采用单副本

可以在报告中表述为：

由于当前后端数据库仍为 SQLite，本次 Kubernetes 部署主要用于验证容器编排方案与部署流程，因此后端暂采用单副本模式运行。后续若升级为支持多实例共享的数据库，再进一步扩展为多副本架构将更合理。

## 七、预期最终架构

改造后的整体结构为：

- 前端：
  - Vue 3 页面构建为静态资源
  - Nginx 容器负责托管页面并代理 `/api`
- 后端：
  - FastAPI 提供登录、推荐、搜索建议、健康检查接口
- 缓存：
  - Redis 提供推荐与搜索建议缓存
- 部署：
  - Docker Compose 负责本地多容器运行
  - Kubernetes 负责服务编排、配置管理和持久化挂载

## 八、总结

本次技术栈增加方案兼顾了三方面要求：

- 与现有项目技术结构兼容
- 改动范围可控
- 实验报告中具有足够的工程化表达力

因此，采用 `K8s + Docker` 作为部署主线，并引入 `Redis` 作为缓存中间件，是当前项目最合适、最稳妥的增强方向。
