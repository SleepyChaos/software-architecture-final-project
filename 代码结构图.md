# 图书管理系统 - 代码结构图

## 项目整体架构

```
图书管理系统
├── 前端 (Vue 3 + TypeScript + Vite)
│   ├── PC端应用 (index.html)
│   └── 移动端应用 (mobile.html)
└── 后端 (FastAPI + SQLite)
    └── backbone/
```

## 前端详细结构

### 1. 入口文件层

```
入口文件
├── index.html (PC端入口)
│   └── <script src="/src/main.ts"></script>
│
└── mobile.html (移动端入口)
    └── <script src="/src/mobile/main.ts"></script>
```

### 2. 应用初始化层

```
应用初始化
├── src/main.ts (PC端初始化)
│   ├── createApp(App)
│   ├── use(createPinia())  // 状态管理
│   ├── use(router)         // 路由
│   └── mount('#app')
│
└── src/mobile/main.ts (移动端初始化)
    ├── createApp(AppMobile)
    ├── use(createPinia())  // 状态管理
    ├── use(router)         // 移动端路由
    └── mount('#app')
```

### 3. 根组件层

```
根组件
├── src/App.vue (PC端根组件)
│   └── <router-view />     // 路由出口
│
└── src/mobile/AppMobile.vue (移动端根组件)
    └── <router-view />     // 路由出口
```

### 4. 路由配置层

```
路由配置
├── src/router/index.ts (PC端路由)
│   ├── createWebHistory()  // HTML5历史模式
│   ├── 路由守卫 (登录检查)
│   └── 路由配置
│       ├── / -> Home.vue
│       ├── /login -> Login.vue
│       ├── /search -> Search.vue
│       ├── /book/:id -> BookDetail.vue
│       ├── /borrow -> Borrow.vue
│       ├── /return -> Return.vue
│       ├── /history -> History.vue
│       └── /profile -> Profile.vue
│
└── src/mobile/router.ts (移动端路由)
    ├── createWebHashHistory() // Hash模式
    ├── 路由配置
    │   ├── / -> HomeMobile.vue
    │   ├── /search -> Search.vue
    │   ├── /book/:id -> BookDetail.vue
    │   ├── /history -> History.vue
    │   ├── /profile -> ProfileMobile.vue
    │   └── /fines -> Fines.vue
```

### 5. 页面组件层

```
页面组件
├── PC端页面 (src/views/)
│   ├── Home.vue          # 首页
│   ├── Login.vue         # 登录页
│   ├── Search.vue        # 搜索页
│   ├── BookDetail.vue    # 图书详情
│   ├── Borrow.vue        # 借阅页
│   ├── Return.vue        # 归还页
│   ├── History.vue       # 借阅历史
│   └── Profile.vue       # 个人中心
│
└── 移动端页面 (src/views/mobile/)
    ├── HomeMobile.vue    # 移动端首页
    ├── ProfileMobile.vue # 移动端个人中心
    └── Fines.vue         # 滞纳金缴纳
```

### 6. 共享组件层

```
共享组件 (src/components/)
├── BookCard.vue          # 图书卡片组件
├── SearchBar.vue         # 搜索栏组件
└── BottomNav.vue         # 底部导航组件 (移动端)
```

### 7. 状态管理层

```
状态管理 (Pinia Stores)
├── auth.ts              # 用户认证状态
│   ├── 用户信息
│   ├── 登录状态
│   └── 认证方法
│
├── books.ts             # 图书数据状态
│   ├── 图书列表
│   ├── 搜索结果
│   └── 图书详情
│
├── loans.ts             # 借阅记录状态
│   ├── 当前借阅
│   ├── 借阅历史
│   └── 借阅操作
│
└── requests.ts          # 请求管理状态
    ├── API请求封装
    ├── 错误处理
    └── 请求拦截
```

### 8. 工具与配置层

```
工具与配置
├── src/utils/           # 工具函数
│   └── index.ts
│
├── src/lib/             # 库文件
│   └── utils.ts
│
├── src/composables/      # 组合式函数
│   └── status.ts
│
├── src/assets/          # 静态资源
│   └── main.css
│
└── 配置文件
    ├── vite.config.ts   # Vite配置
    ├── package.json     # 项目依赖
    ├── tailwind.config.js # Tailwind CSS配置
    └── tsconfig.json     # TypeScript配置
```

## 数据流向图

```
用户交互
    ↓
页面组件 (Views)
    ↓
共享组件 (Components)
    ↓
状态管理 (Stores) ←→ API请求 (后端)
    ↓
UI更新
```

## 路由跳转流程

```
启动应用
    ↓
加载入口HTML (index.html 或 mobile.html)
    ↓
执行main.ts (PC端) 或 mobile/main.ts (移动端)
    ↓
创建Vue应用实例
    ↓
挂载根组件 (App.vue 或 AppMobile.vue)
    ↓
加载路由配置
    ↓
根据URL匹配路由组件
    ↓
渲染页面组件
```

## PC端与移动端共享关系

```
共享部分:
├── 状态管理 (stores/)
├── 部分页面组件 (Search.vue, BookDetail.vue, History.vue)
├── 共享组件 (BookCard.vue, SearchBar.vue)
├── 工具函数 (utils/, lib/)
└── 样式文件 (assets/)

独立部分:
├── 入口文件 (index.html vs mobile.html)
├── 初始化文件 (main.ts vs mobile/main.ts)
├── 根组件 (App.vue vs AppMobile.vue)
├── 路由配置 (router/index.ts vs mobile/router.ts)
└── 特定页面组件 (PC端专属 vs 移动端专属)
```

## 技术栈总结

```
前端技术栈:
├── Vue 3 (Composition API)
├── TypeScript
├── Vite (构建工具)
├── Vue Router (路由管理)
├── Pinia (状态管理)
├── Tailwind CSS (样式框架)
└── Axios/Fetch (HTTP请求)

后端技术栈:
├── FastAPI (Python Web框架)
├── SQLite (数据库)
└── SQLAlchemy (ORM)
```