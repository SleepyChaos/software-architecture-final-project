# 项目启动说明

本文档用于指导本项目的实际启动与访问。为了适配不同展示场景，项目提供三种启动方式：

- 原生开发方式
- Docker Compose 方式
- Kubernetes 本地部署方式

如果只是日常开发，推荐优先使用“原生开发方式”。
如果需要展示工程化部署能力，推荐使用“Docker Compose”或“Kubernetes 本地部署方式”。

## 一、启动前准备

### 1. 代码位置

请确保当前终端位于项目根目录：

```bash
cd /Users/chaos/Documents/软件架构期末大作业增加技术栈/软件架构期末大作业
```

### 2. 依赖要求

根据不同启动方式，需要具备以下环境：

#### 原生开发方式

- Node.js
- npm
- Python 3
- pip

#### Docker Compose 方式

- Docker
- Docker Compose

#### Kubernetes 本地部署方式

- Docker
- kubectl
- 本地 Kubernetes 集群
  - 例如：`minikube`、`kind`、`k3d`

### 3. 测试账号

- 普通登录：`001 / 123456`
- 人脸模拟登录：`face-user / face`

## 二、原生开发方式

这种方式适合平时开发和调试。

### 1. 启动后端

打开一个终端窗口，执行：

```bash
cd backbone
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

启动成功后，后端地址为：

- 接口根地址：[http://127.0.0.1:8000](http://127.0.0.1:8000)
- 接口文档：[http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

### 2. 启动 PC 端前端

打开第二个终端窗口，执行：

```bash
npm install
npm run dev
```

默认访问地址通常为：

- [http://127.0.0.1:5173](http://127.0.0.1:5173)

### 3. 启动移动端前端

如果需要同时演示移动端，再打开第三个终端窗口，执行：

```bash
npm install
npm run dev:mobile
```

默认访问地址通常为：

- [http://127.0.0.1:5174/mobile.html](http://127.0.0.1:5174/mobile.html)

### 4. 原生方式说明

- 前端默认通过 `.env.development` 中的 `VITE_API_BASE_URL=http://127.0.0.1:8000` 访问本地后端。
- 如果本地没有启动 Redis，不会影响系统基本使用。
- 推荐图书和搜索建议接口会自动降级到后端本地 mock 逻辑。

## 三、Docker Compose 启动方式

这种方式适合展示“容器化部署”和“多服务协同”。

### 1. 启动命令

在项目根目录执行：

```bash
docker compose -p library-system up --build
```

说明：

- `-p library-system` 是显式指定项目名。
- 当前目录名中包含中文，显式指定项目名可以避免 Docker Compose 默认项目名解析异常。

### 2. 访问地址

启动成功后可访问：

- PC 端入口：[http://localhost:8080](http://localhost:8080)
- 移动端入口：[http://localhost:8080/mobile.html](http://localhost:8080/mobile.html)

### 3. 容器组成

该方式会启动以下三个服务：

- `frontend`
  - 负责托管前端页面
  - 使用 Nginx 提供静态资源
  - 将 `/api` 代理到后端
- `backend`
  - 运行 FastAPI
  - 提供登录、推荐图书、搜索建议、健康检查接口
- `redis`
  - 为推荐图书与搜索建议提供缓存

### 4. 数据持久化

SQLite 数据会保存到 Docker 命名卷中：

- `library-system_sqlite_data`

这意味着容器重建后，数据库文件不会直接丢失。

### 5. 停止命令

停止运行：

```bash
docker compose -p library-system down
```

如果希望同时删除卷：

```bash
docker compose -p library-system down -v
```

## 四、Kubernetes 本地部署方式

这种方式适合展示“容器编排、服务管理、配置管理和持久化挂载能力”。

### 1. 构建镜像

在项目根目录执行：

```bash
docker build -t library-system-frontend:latest .
docker build -t library-system-backend:latest -f backbone/Dockerfile .
```

### 2. 将镜像导入本地集群

如果你的本地 K8s 集群与宿主 Docker 环境隔离，还需要把镜像导入集群。

不同平台命令不同，例如：

#### minikube

```bash
minikube image load library-system-frontend:latest
minikube image load library-system-backend:latest
minikube image load library-system-redis:latest
```

#### kind

```bash
kind load docker-image library-system-frontend:latest --name library-system
kind load docker-image library-system-backend:latest --name library-system
kind load docker-image library-system-redis:latest --name library-system
```

#### k3d

```bash
k3d image import library-system-frontend:latest
k3d image import library-system-backend:latest
k3d image import library-system-redis:latest
```

### 3. 应用 K8s 清单

```bash
kubectl apply -f k8s/
```

### 4. 查看运行状态

```bash
kubectl get pods
kubectl get svc
```

### 5. 本地访问前端

```bash
kubectl port-forward svc/library-frontend 8080:80
```

如果本机 `8080` 端口已被 Docker Compose 占用，可以改用：

```bash
kubectl port-forward svc/library-frontend 8081:80
```

访问地址：

- PC 端入口：[http://localhost:8080](http://localhost:8080)
- 移动端入口：[http://localhost:8080/mobile.html](http://localhost:8080/mobile.html)

### 6. K8s 组成说明

K8s 清单包括：

- `frontend-deployment.yaml`
- `frontend-service.yaml`
- `backend-deployment.yaml`
- `backend-service.yaml`
- `redis-deployment.yaml`
- `redis-service.yaml`
- `backend-configmap.yaml`
- `backend-pvc.yaml`

说明：

- 前端、后端、Redis 目前都采用单副本
- 后端通过 `ConfigMap` 读取 Redis 和缓存 TTL 配置
- 后端通过 `PVC` 挂载 SQLite 数据
- 后端配置了 `/healthz` 健康检查

### 7. 停止与清理

删除部署：

```bash
kubectl delete -f k8s/
```

## 五、推荐的启动顺序

### 日常开发

推荐顺序：

1. 启动后端
2. 启动 PC 前端
3. 如有需要再启动移动端前端

### 课程展示

如果重点展示工程化部署：

1. 启动 Docker Compose 或本地 K8s
2. 打开 PC 端入口
3. 演示登录
4. 演示首页推荐和搜索建议
5. 说明 Redis 缓存、Nginx 反代、K8s 编排和 PVC 的作用

## 六、常见问题排查

### 1. 前端打不开或空白

检查：

- Node 依赖是否安装完成
- 是否执行了正确的启动命令
- Vite 端口是否被占用

### 2. 登录失败

检查：

- 后端是否已启动
- 前端 `VITE_API_BASE_URL` 是否指向正确地址
- 测试账号是否输入正确

### 3. 搜索建议或推荐接口失败

检查：

- 后端是否正常运行
- Redis 是否已启动

说明：

即使 Redis 没启动，系统也会自动降级，一般不会导致页面完全不可用。

### 4. Docker Compose 报项目名错误

解决方式：

不要直接执行 `docker compose up`，应使用：

```bash
docker compose -p library-system up --build
```

### 5. Kubernetes 无法访问

检查：

- 本地 K8s 集群是否已经启动
- 镜像是否导入到集群
- Pod 是否处于 `Running` 状态
- 是否已执行 `kubectl port-forward`

## 七、当前方案的限制

启动前需要理解当前方案并不是完整生产级系统，主要限制包括：

- 后端数据库仍为 SQLite，因此后端在 K8s 中仍以单副本方式运行
- Redis 当前只做缓存，不做分布式会话、分布式锁和消息队列
- 借阅、续借、预约等大量业务仍以本地模拟逻辑为主

## 八、建议

如果你的目标是：

- 快速调试代码：用原生开发方式
- 展示容器化部署：用 Docker Compose
- 展示编排与扩展思路：用 Kubernetes 本地部署方式

如果你后面愿意，我还可以继续为这份文档再补一版“答辩时怎么讲”的启动演示话术。
