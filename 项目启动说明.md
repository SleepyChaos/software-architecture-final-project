# 项目启动说明

本文档用于指导本项目的实际启动与访问。为了适配不同展示场景，项目提供三种启动方式：

- 原生开发方式
- Docker Compose 方式
- Kubernetes 本地部署方式

如果只是日常开发，推荐优先使用"原生开发方式"。
如果需要展示工程化部署能力，推荐使用"Docker Compose"或"Kubernetes 本地部署方式"。

## 技术栈

| 类别 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Vue 3 + TypeScript + Vite | 双入口 (PC端 + 移动端) |
| 状态管理 | Pinia | 4 个 store |
| CSS | Tailwind CSS | 响应式布局 |
| 后端框架 | FastAPI (Python) | RESTful API |
| ORM | SQLAlchemy | 数据库抽象层 |
| 数据库 | PostgreSQL (pgvector) | 替代原 SQLite |
| 缓存 | Redis | 推荐图书 + 搜索建议缓存 |
| 可视化 | ECharts | 数据统计图表 |
| 反向代理 | Nginx | 静态资源托管 + API 代理 + 负载均衡 |
| 容器化 | Docker + Docker Compose | 四容器编排 |
| 编排 | Kubernetes | 8 个 K8s 清单 |

## 一、启动前准备

### 1. 依赖要求

#### 原生开发方式

- Node.js + npm
- Python 3 + pip
- PostgreSQL (本地安装或 Docker 容器)

#### Docker Compose 方式

- Docker + Docker Compose

#### Kubernetes 本地部署方式

- Docker
- kubectl
- 本地 Kubernetes 集群（minikube、kind、k3d 均可）

### 2. 测试账号

- 普通登录：`001 / 123456`
- 人脸模拟登录：`face-user / face`

## 二、原生开发方式

### 1. 准备 PostgreSQL

确保本地 PostgreSQL 已启动，创建数据库和用户：

```sql
CREATE USER library WITH PASSWORD 'library';
CREATE DATABASE library OWNER library;
```

如果使用 Docker 单独运行 PostgreSQL：

```bash
docker run -d --name postgres \
  -e POSTGRES_USER=library \
  -e POSTGRES_PASSWORD=library \
  -e POSTGRES_DB=library \
  -p 5432:5432 \
  pgvector/pgvector:pg17-trixie
```

### 2. 启动后端

```bash
cd backbone
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

启动成功后：

- 接口地址：[http://127.0.0.1:8000](http://127.0.0.1:8000)
- API 文档：[http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

后端启动时会自动建表并写入种子用户。

### 3. 启动 PC 端前端

```bash
npm install
npm run dev
```

默认访问：[http://127.0.0.1:5173](http://127.0.0.1:5173)

### 4. 启动移动端前端

```bash
npm run dev:mobile
```

默认访问：[http://127.0.0.1:5174/mobile.html](http://127.0.0.1:5174/mobile.html)

### 5. 注意事项

- 前端通过 `.env.development` 中的 `VITE_API_BASE_URL=http://127.0.0.1:8000` 访问本地后端
- 未启动 Redis 不影响基本使用，推荐和搜索建议会自动降级

## 三、Docker Compose 启动方式

### 1. 先构建前端

Docker 构建需要已生成的前端 `dist` 目录：

```bash
npm install
npm run build
```

### 2. 构建并启动

```bash
docker compose build --pull never
docker compose up -d --scale backend=3
```

参数说明：

- `--pull never`：跳过远程镜像检查，使用本地已有镜像
- `--scale backend=3`：启动 3 个后端实例，配合 Nginx 实现负载均衡

### 3. 访问地址

- PC 端：[http://localhost:8080](http://localhost:8080)
- 移动端：[http://localhost:8080/mobile.html](http://localhost:8080/mobile.html)
- 数据统计：[http://localhost:8080/statistics](http://localhost:8080/statistics)

### 4. 容器组成

| 服务 | 容器名 | 镜像 | 端口 |
|------|--------|------|------|
| frontend | library-frontend | 本地构建 (Nginx) | 8080 |
| backend (x3) | 自动命名 | 本地构建 (FastAPI) | 8000 (内部) |
| db | library-db | pgvector/pgvector:pg17-trixie | 5432 |
| redis | library-redis | redis:7-alpine | 6379 (内部) |

### 5. 负载均衡

Nginx 配置了 `upstream backend_pool` 和 Docker DNS resolver (`127.0.0.11`)，3 个后端实例通过轮询方式分发请求。验证方式：

```bash
docker compose logs backend | head -20
```

### 6. 数据持久化

PostgreSQL 数据保存在 Docker 命名卷 `pgdata` 中，容器重建不会丢失。

### 7. 停止

```bash
docker compose down
```

如需同时删除数据卷：

```bash
docker compose down -v
```

## 四、Kubernetes 本地部署方式

### 1. 先构建前端

```bash
npm install
npm run build
```

### 2. 构建镜像

```bash
docker build -t library-system-frontend:latest .
docker build -t library-system-backend:latest -f backbone/Dockerfile .
```

### 3. 导入镜像到本地集群

不同平台命令不同：

**minikube**：

```bash
minikube image load library-system-frontend:latest
minikube image load library-system-backend:latest
```

**kind**：

```bash
kind load docker-image library-system-frontend:latest --name library-system
kind load docker-image library-system-backend:latest --name library-system
```

**k3d**：

```bash
k3d image import library-system-frontend:latest
k3d image import library-system-backend:latest
```

### 4. 应用 K8s 清单

```bash
kubectl apply -f k8s/
```

### 5. 查看运行状态

```bash
kubectl get pods
kubectl get svc
```

后端默认 3 个副本，可通过 `kubectl get pods` 确认。

### 6. 访问

```bash
kubectl port-forward svc/library-frontend 8080:80
```

访问地址：

- PC 端：[http://localhost:8080](http://localhost:8080)
- 移动端：[http://localhost:8080/mobile.html](http://localhost:8080/mobile.html)

### 7. K8s 清单组成

| 文件 | 说明 |
|------|------|
| frontend-deployment.yaml | 前端 Deployment |
| frontend-service.yaml | 前端 ClusterIP Service |
| backend-deployment.yaml | 后端 Deployment (replicas: 3) |
| backend-service.yaml | 后端 ClusterIP Service，负载均衡 |
| backend-configmap.yaml | 后端环境变量配置 |
| postgres-deployment.yaml | PostgreSQL Deployment |
| postgres-service.yaml | PostgreSQL ClusterIP Service |
| postgres-pvc.yaml | PostgreSQL 持久化存储 |
| redis-deployment.yaml | Redis Deployment |
| redis-service.yaml | Redis ClusterIP Service |

### 8. 停止与清理

```bash
kubectl delete -f k8s/
```

## 五、常见问题排查

### 1. Docker 构建报网络错误

Docker 构建使用 `--pull never` 跳过远程镜像拉取。如果新机器首次构建，需先确保基础镜像已本地存在：

```bash
docker pull python:3.11-slim
docker pull nginx:1.27-alpine
docker pull pgvector/pgvector:pg17-trixie
docker pull redis:7-alpine
```

如果 Docker Hub 网络不通，需在 Docker Desktop 设置中配置国内镜像源。

### 2. npm 构建报错

项目根目录下的 `.npmrc` 已配置淘宝镜像。如果仍失败，手动执行：

```bash
npm install --registry=https://registry.npmmirror.com
npm run build
```

### 3. Docker 构建找不到 dist

需先执行 `npm run build` 生成 dist 目录。

### 4. 登录失败

检查：

- 后端是否已启动
- PostgreSQL 是否正常运行
- 测试账号是否输入正确
- 前端 `VITE_API_BASE_URL` 指向地址是否正确

### 5. 端口冲突

如果 5432 端口已被占用（本机已有 PostgreSQL），Docker Compose 启动会失败。修改 `docker-compose.yml` 中 db 的端口映射或停止本机 PostgreSQL。

## 六、当前方案说明

启动前需要理解当前方案的定位：

- 数据库已从 SQLite 升级为 PostgreSQL，支持多副本后端并发读写
- Redis 承担推荐和搜索建议缓存，不可用时自动降级
- Nginx 基于 upstream + Docker DNS 实现多副本负载均衡
- ECharts 在 `/statistics` 页面展示图书分类占比、月度借阅量和热门图书排行
- 借阅、续借、预约等业务仍以前端本地模拟逻辑为主
