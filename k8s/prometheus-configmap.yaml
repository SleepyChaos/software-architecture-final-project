apiVersion: v1
kind: ConfigMap
metadata:
  name: library-prometheus-config
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      evaluation_interval: 15s

    scrape_configs:
      - job_name: library-backend
        metrics_path: /metrics
        static_configs:
          - targets:
              - backend:8000