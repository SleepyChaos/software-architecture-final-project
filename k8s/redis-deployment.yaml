apiVersion: apps/v1
kind: Deployment
metadata:
  name: library-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: library-redis
  template:
    metadata:
      labels:
        app: library-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 6379
