apiVersion: v1
kind: ConfigMap
metadata:
  name: library-elasticsearch-config
data:
  elasticsearch.yml: |
    cluster.name: library-elasticsearch
    node.name: library-elasticsearch
    network.host: 0.0.0.0
    discovery.type: single-node
    xpack.security.enabled: false