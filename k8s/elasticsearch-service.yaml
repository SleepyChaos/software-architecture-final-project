apiVersion: v1
kind: Service
metadata:
  name: library-elasticsearch
spec:
  selector:
    app: library-elasticsearch
  ports:
    - port: 9200
      targetPort: 9200
      protocol: TCP
  type: ClusterIP