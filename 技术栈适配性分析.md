# 图书管理系统技术栈适配性分析

## 项目当前状态

当前项目是一个轻量级前后端分离图书管理系统，整体结构如下：

- 前端：Vue 3 + TypeScript + Vite + Pinia + Vue Router + Tailwind CSS
- 后端：FastAPI + SQLAlchemy + SQLite
- 业务实现特征：
  - 登录已接入后端
  - 图书、借阅、预约、续借等多数业务仍以本地模拟数据为主
  - 项目适合作为课程作业进行工程化增强，而不适合一次性重构为重型分布式系统

本次选型的目标不是盲目堆砌技术，而是在现有项目规模下增加合理的工程化能力，并为实验报告提供清晰、可信的架构演进路径。

## 候选技术适配性结论

### 一、推荐选用

#### 1. Docker

适配度：高

原因：

- 当前项目天然分为前端与后端两个运行单元，适合容器化封装。
- Docker 可以统一开发环境、运行环境和展示环境，降低本地运行差异。
- 后续如果引入 Redis、Nginx、Kubernetes，Docker 是最自然的基础设施。
- 在课程展示中，Docker 能明显体现“工程化部署能力”。

结论：

推荐选用，并作为后续 K8s 编排的前置基础。

#### 2. Redis

适配度：高

原因：

- Redis 可直接以独立容器运行，与 Docker 和 K8s 组合自然。
- 首版只需承担缓存职责，就能提升系统层次而不必大改业务模型。
- 可优先用于首页推荐、搜索建议等读多写少场景，改动少、效果明确。
- 相比会话共享、分布式锁、消息队列等高级用法，缓存场景更符合当前项目规模。

结论：

推荐选用，且首版定位为缓存中间件。

#### 3. ECharts / HighCharts

适配度：中高

原因：

- 适合用于个人中心、首页等位置展示借阅趋势、分类分布、热门图书统计。
- 用户可视化效果强，适合做课程展示。
- 但其价值主要体现在界面展示层，对系统工程化提升不如 Docker / Redis / K8s 明显。

结论：

可作为后续增强项，但不作为本轮首选。

#### 4. Elasticsearch

适配度：中高

原因：

- 项目已有搜索页与搜索建议交互，理论上非常适合做全文检索升级。
- 但当前项目业务数据仍多为前端模拟，如果直接引入 Elasticsearch，需要先补较多后端接口与数据同步逻辑。

结论：

技术上很适合，但改造范围大于 Redis，因此暂不优先。

### 二、可引入但当前不优先

#### 1. Nginx

适配度：中高

原因：

- 非常适合承担前端静态资源托管和 `/api` 反向代理。
- 与 Docker 配合自然，也有利于后续部署到 K8s。
- 但它更像 Docker / K8s 落地时的配套组件，而不是单独作为课程技术栈重点。

结论：

在实现 Docker 化时推荐一起引入，但不单独作为主选型陈述。

#### 2. MQ / RabbitMQ / Kafka

适配度：中

原因：

- 理论上可以用于预约通知、超期提醒、异步日志等。
- 但当前项目还没有稳定的后端业务链路，直接上消息队列会造成“技术先行、业务落空”。
- RabbitMQ 比 Kafka 更贴合当前场景，但整体改造量仍不小。

结论：

可以作为扩展思路写进报告，但不适合作为本轮主实现。

#### 3. Prometheus

适配度：中

原因：

- 适合监控接口数量、延迟、错误率，工程味很强。
- 但如果没有较完整的容器和服务体系，监控展示价值有限。

结论：

可作为未来可观测性扩展方向，不优先落地。

### 三、当前项目不建议选用

#### 1. Kubernetes 单独选用

适配度：中

原因：

- 对当前项目体量来说，K8s 确实偏重。
- 但如果与 Docker 配合，并且明确说明“先轻量使用、为后续扩容预留能力”，则仍可作为课程选型成立。

结论：

不建议单独上 K8s，但推荐与 Docker 组合成完整部署技术栈。

#### 2. 数据库读写分离

适配度：低

原因：

- 当前项目数据库仍是 SQLite，不存在主从结构。
- 读写分离通常建立在 MySQL、PostgreSQL 等可扩展数据库之上。

结论：

当前阶段不适合。

#### 3. Spring Security / Shiro

适配度：低

原因：

- 这两个都是 Java 生态安全框架。
- 当前项目后端是 Python FastAPI，引入它们意味着更换后端技术路线。

结论：

不适合当前项目。

#### 4. Sentinel / Nacos

适配度：低

原因：

- 它们更常用于 Spring Cloud 微服务体系。
- 当前项目既不是微服务，也没有服务注册、配置中心、限流熔断等成熟需求。

结论：

不适合当前项目。

#### 5. ZooKeeper

适配度：低

原因：

- 常配合 Kafka 或分布式协调场景使用。
- 当前项目没有分布式协调需求。

结论：

不适合当前项目。

#### 6. Memcached

适配度：低

原因：

- 与 Redis 相比，Memcached 功能更少，生态更弱，扩展性较弱。
- 既然项目已决定选缓存中间件，Redis 的综合性价比更高。

结论：

不推荐，优先 Redis。

#### 7. AngularJS / React

适配度：低

原因：

- 当前前端已是 Vue 3 项目。
- 改用 React 或 AngularJS 意味着重写前端，收益远小于成本。

结论：

不适合当前项目。

#### 8. Django

适配度：低

原因：

- 虽然同属 Python 生态，但当前后端已采用 FastAPI。
- 切换到 Django 会造成重复建设。

结论：

不适合当前项目。

#### 9. Web Service / DOM / Node.js / RESTful API

适配度说明：

- Web Service：偏传统，不符合当前前后端分离项目风格。
- DOM：属于前端基础能力，不适合作为独立技术栈增强项。
- Node.js：当前仅作为前端构建环境存在，无法体现独立选型价值。
- RESTful API：严格来说属于接口设计规范，而不是新增中间件或部署技术栈。

结论：

这些不作为本轮新增技术栈主项。

## 最终选型结论

综合当前项目规模、已有技术结构、改动成本与实验报告表现力，本次最终选择如下：

- 主技术栈：Docker + Kubernetes
- 附加技术栈：Redis

## 选型理由

### 为什么不选其他技术

- 很多技术属于异构生态，如 Spring Security、Shiro，不适配现有 Python 后端。
- 很多技术适合更大规模系统，如 Nacos、Sentinel、ZooKeeper、读写分离，当前项目缺乏必要前提。
- 一些技术虽然可用，但首版落地收益不如 Redis 明显，如 MQ、Prometheus、Elasticsearch。

### 为什么当前项目适合“轻量使用 K8s”

- 当前项目虽然体量小，但已经具备前端、后端、缓存等多个部署单元。
- 通过 Docker 封装后，再用 K8s 管理这些容器，可以自然展示服务编排、配置管理、健康检查与后续扩容入口。
- 报告中可以明确说明：当前先采用单副本轻量部署，是为了在现有规模下验证工程化方案，并为未来数据库升级与多副本扩容打基础。

### 为什么 Redis 首版只做缓存

- 缓存是改造成本最低、收益最直观的 Redis 用法。
- 首页推荐与搜索建议都是读多写少场景，适合作为首版落点。
- 如果一开始就做登录态共享、分布式锁或预约幂等，会明显扩大后端改造范围，不利于课程作业稳妥推进。

## 后续实施方向概览

实施将按以下顺序推进：

1. 统一文档和选型结论
2. 收口前端登录链路到本地后端
3. 为前后端与 Redis 编写 Docker 部署方案
4. 为推荐与搜索建议增加 Redis 缓存
5. 为前端、后端、Redis 编写 Kubernetes 编排清单
6. 更新 README 和部署说明，形成完整实验材料
